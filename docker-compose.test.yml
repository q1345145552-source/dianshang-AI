# 测试环境覆盖
#
# 特点：只暴露应用端口，三个基础设施留在容器内网，外面连不进来。
#       用独立项目名跑，容器和数据卷跟开发环境完全隔开。
#
# 起：make up ENV=test

services:
  api:
    ports:
      - "${API_PORT}:3001"

  web:
    ports:
      - "${WEB_PORT}:3000"
